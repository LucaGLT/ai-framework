# DEPRECATED — use canonical registry at repository root:
#   specs/parameters.yml
#
# This stub remains so legacy paths in prompts resolve correctly during migration.
# All new references must point to specs/parameters.yml.

redirect: "../../../specs/parameters.yml"
